import { Context, Plugin, Plugins } from '@reactant/types';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function loadPlugins(_context: Context): Plugins {
  // TODO 3
  return ({} as unknown) as Plugins;
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function loadPlugin(_context: Context): Plugin {
  // TODO 2
  return ({} as unknown) as Plugin;
}
